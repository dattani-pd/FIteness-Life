import 'package:get/get.dart';
import '../controllers/wger_controller.dart';

class WgerBinding extends Bindings {
  @override
  void dependencies() {
   // Get.lazyPut<WgerController>(() => WgerController());
  }
}